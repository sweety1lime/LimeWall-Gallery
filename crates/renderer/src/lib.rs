//! Wallpaper playback and the daemon behind the two renderer executables:
//! the `renderer` CLI and the windowless `limewall-daemon` used by autostart.

pub mod daemon;
pub mod playback;
pub mod playlist;
