[package]
name = "mpv"
description = "Thin hand-written libmpv client bindings, loaded at runtime"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
libloading = "0.8"
thiserror.workspace = true
